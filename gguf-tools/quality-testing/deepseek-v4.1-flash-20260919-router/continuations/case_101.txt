# Memory Safety Audit

## The unsafe function

```c
int checked_get(const int *a, int n, int i) { if (i <= n) return a[i]; return 0; }
```

Every one of the `limit_N` functions is trivially memory-safe (they only touch a